/**
 * Placeholder for span-shaping helpers (e.g. converting raw OTLP spans
 * into the `Span` shape from @agentops/shared). Implemented alongside
 * backend/services/telemetry_ingest.py in a later phase.
 */
export {};
